import loadingSpinner from './images/loadingSpinner.gif'


const Spinner = () => {
    return (
        <div>
            <img src={loadingSpinner}  alt="Loading graphic"/>
        </div>
    )
}

export default Spinner